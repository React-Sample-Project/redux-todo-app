export const AVAILABLE_COLORS = ['green', 'blue', 'orange', 'purple'];
export const STATUS_FILTERS = {
    All: 'all',
    Active: 'active',
    Completed: 'completed',
}
export const capitalize = (s) => s[0].toUpperCase() + s.slice(1);